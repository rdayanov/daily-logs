.form {
    height: 100vh;
    width: 100vw;
    display: flex;
    flex-flow: column nowrap;
    align-items: start;
    justify-content: start;
    gap: 1rem;
    padding: 4rem 0 0 4rem;
}

.drag-handle {
    width: 100%;
    max-width: fit-content;
}

.drag-drop-area {
    height: 2rem;
}
